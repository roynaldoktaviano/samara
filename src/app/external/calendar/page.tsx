import { PublicCalendarView } from '@/components/calendar/PublicCalendarView'

export const metadata = { title: 'Vessel Schedule — Samara Liveaboard' }

export default function ExternalCalendarPage() {
  return <PublicCalendarView />
}
